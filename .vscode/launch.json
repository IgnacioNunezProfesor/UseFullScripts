{
    "configurations": [
        {
            "name": "PowerShell Launch UpdateAll",
            "type": "PowerShell",
            "request": "launch",
            "script": "${workspaceFolder}/UpdateAll.ps1",
            "cwd": "${cwd}"
        }
    ]
}